/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['fireship.io','img.freepik.com','cdn.iconscout.com'],
},
}

module.exports = nextConfig
